# 2026-09-19 之前的设计与入口约定

本目录保存完整推送前的 CLAUDE、设计规范、全模块 PRD 和站点地图，保留旧页面的背景与历史业务解释。

当前主线依据 `SPEC-SUIYIN-ADMIN-051@1.1.0` 和真实后台采样。此处的微信绿/公众号白头、56px 顶栏、200px 侧栏、旧十六租户集合、同页切换后全部 iframe 重载、未采页面统一占位、非成都 AI 默认空态均为被替代的历史安排，不作为当前实现指令。

统计业务定义不能由视觉更新推翻；009/042 精确版本仍通过当前 SDD 的依赖入口引用。
