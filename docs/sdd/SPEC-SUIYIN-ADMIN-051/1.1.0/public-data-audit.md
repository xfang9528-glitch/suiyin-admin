# 公开样本处理审计

- 日期：2026-09-19
- 范围：`suiyin-admin/prototype/data/**`、案例示意图片及可重复执行的 `prototype/sanitize-public-data.mjs`
- 依据：本次完整推送授权；SPEC-SUIYIN-ADMIN-051 的本地 Mock / 联系信息脱敏边界；工作区 Constitution Article X 的公开仓数据边界。
- 结论：已移除发布数据中的客户身份与原始整页文本，保持原型的记录量、字段、菜单、业务状态及来源标注。

## 已实施

1. 客户/好友姓名和昵称、客户备注、群名、预约客户、收入/预付关联客户、宠物名称使用稳定演示名称；同一原值在表单、下拉及明细中保持一致替换。
2. 平台“全部帐号状态”中的在线/掉线账号列表使用演示工作账号名称；环境展示与数量保持原状。
3. 737 页 `.text` 不再保存原始整页采集文本：表格页从脱敏后的表头与单元格重建；话术页使用 8 个明确演示分类保持非空操作；其他非表格页只保留页面标题。
4. 租户、品牌、菜单、员工与日常运营账号展示保留。`navigation-snapshot.json` 整文件排除于身份替换，处理前后字节一致；公司/品牌/环境元信息显式保护。客户与员工重名时，客户列化名，销售姓名/销售名称/所在账号/工作账号等运营列按角色保留；表单和下拉只在客户身份上下文替换，不修改业务组织名称。
5. 两张来源案例照片退出公开原型资产，改用相同画布尺寸的中性 SVG（800×1422、1440×1919）；编辑器引用已同步。
6. 每页增加 `publicDataPolicy: public-demo-identities-v1`，并更新样本 revision，让现有浏览器加载新公开样本。

## 检查结果

| 检查 | 结果 |
|---|---|
| 租户 / 页面 / 表格行数 | 15 / 737 / 11245，处理前后保持一致 |
| 来源元数据对比 | 6633 项保持一致 |
| 导航整文件对比 | 与处理前备份字节一致，租户名称/品牌/菜单完整保留 |
| 员工、运营账号与菜单单元格对比 | 14413 个单元格保持一致 |
| 身份字段检查 | 2405 个单元格通过演示名称/安全空值检查 |
| 密钥字段检查 | 36 个字段均为空或 `[已隐藏]` |
| 明文手机号 / JWT / provider key / 私钥特征 | 发布 data 目录未检出 |
| 原始文本残留 | 所有页面 `.text` 均与公开重建结果一致 |
| 重复执行 | 第二次运行 data 文件零字节变化 |
| 语法 | `node --check prototype/sanitize-public-data.mjs` 通过 |
| 导航校验 | 15 租户、737 入口、103 条点击证据，sourceTreeParity / scopeExclusions 全通过 |
| 内容校验 | 737 页、83 路由、11245 行，来源对账 / 图表计算 / 隔离 / 语法 / 静态边界全通过 |
| 原客户/群身份复扫 | 目标身份列无残留；重名的运营角色列按上述保留规则处理 |

只读复核命令：

```powershell
node prototype/sanitize-public-data.mjs --check
```

首次处理要求显式传入仓外备份目录；脚本拒绝将私有输入备份放入本仓。现有原始输入备份仅供本地核对，不属于远端 SDD、公开证据或原型运行资产，不得复制进发布包。

本检查验证的是可公开数据处理与静态结构完整性，不替代浏览器视觉验收。

修正记录：首次范围过宽的替换曾将一个与客户群同名的租户名称错误化名。已从私有输入恢复完整导航并排除整文件处理，同时收窄表单/下拉替换范围、保护品牌元数据；重新运行完整处理、字节对比、导航和内容校验均通过。当前报告结论只适用于该修正后的发布候选。
