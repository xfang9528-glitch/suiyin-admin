# 使用统计同视口几何回量

2026-09-20，依据用户第1图右侧实站与主代理最终 `renders/salesMessageUsage.png`。

- 原图尺寸3827×2108；右侧应用内容起点为物理像素 `(1927,103)`，实际白色应用画布从此开始。为对照保留到图片下沿，比较视口为1900×2005物理像素，即1520×1604 CSS px、DPR1.25。
- 当前截图已经按1520×1604 CSS px、DPR1.25重拍，且取消 headless 默认 `--hide-scrollbars`。iframe 1240 CSS px、文档可用1232 CSS px，保留8 CSS px纵滚动槽。
- 以下均为截取原点后的**物理像素**，不是CSS像素。来源数据是09-20，原型数据是09-18，人物顺序、行组数量与总值不作为同一内容断言。

| 可比锚点 | 实站 | 第一轮 | 当前 | 当前残差 |
|---|---:|---:|---:|---:|
| 筛选区顶边y | 156 | 156 | 156 | 0 |
| 筛选区底边y | 241 | 241 | 241 | 0 |
| 标题字形y范围 | 292–310 | 290–308 | 292–310 | 0 |
| 表头底横线y | 386 | 约384 | 386 | 0 |
| 搜索青绿主体x范围 | 1495–1586 | 1464–1555 | 1498–1586 | 左+3／右0 |
| 统计卡片左边x | 334 | 335 | 335 | +1 |
| 统计卡片右边x | 1850 | 1874 | 1854 | +4 |
| # 表头可见字形x范围 | 371–379 | 372–380 | 372–380 | +1 |
| 销售表头起始x | 461 | 462 | 462 | +1 |
| 部门表头起始x | 755 | 754 | 754 | -1 |
| 消息数排序箭头末端x | 1319 | 1324 | 1324 | +5 |
| 聊天好友数排序箭头末端x | 1610 | 1614 | 1614 | +4 |

第一轮截图viewport为1528×1588，因此右边界含窗口差异，不能直接全部当CSS误差。当前已统一窗口，残差表以最终截图为准。

## 已完成的窄修复

1. 部门下拉从实际200恢复220 CSS px：原自定义选择器优先级低于 `.admin-content .filter-panel .field>.live-select`，只改声明值不生效；现提高专用选择器权重。
2. 快捷日按钮恢复15px左右内边距、60px最小宽度，消除原约5 CSS px组宽不足。
3. 统计标题恢复16px ×1.6行高；标题字形y与源一致，表头底线也落在源y386。
4. 表格行高恢复采样40.1 CSS px（line-height23.1 + 上下padding16 + 横线1），避免原40px的逐行累计误差。
5. 仅使用页设置8 CSS px纵滚动槽，不修改其他页面或强行给全部表格相同高度。

安装版CSS摘要：`260909050677D63384F3F314EE574852D4919A1A06A2493CC176AFB2176AD9FB`。本轮JS无进一步变化。

## 尚存差异与复核边界

- 卡片水平外边界仍约1–4物理px差；筛选组末端约2–3物理px差。没有再追加分数像素magic offset，以免把浏览器边界栅格化和内部滚动层差异写死为页面规则。
- 两个数值表头排序箭头末端仍约4–5物理px差，正文数字右端约1–3px。更可能是来源caret容器内部留白与自绘caret不同，不能将整列一起左移，否则破坏已基本一致的正文对齐。
- 当前信息图标按截图重建，尚未取得当前实站SVG原始字节；不是字节级相同。
- 源图右边可见外层与卡片附近两条纵向滚动轨道；本地当前是页面纵向滚动。自然长表已消除旧版被强行截短的表格与悬浮总计，但实站两层滚动的真实交互尚需只读浏览器复核，不能仅按一张图推断容器滚动行为。
- 源图表格第一组为09-20的刘佳欣两部门加合计；当前09-18第一组冯琪单部门，所以后续逐行边界本来就不应重叠。所有样本仍明确使用原日期，没有改成“当前真实数据”。

机器可复核坐标：`usage-geometry-before.json`、`usage-geometry-after.json`、`usage-geometry-final.json`。`measure-usage.py` 只读取原始截图像素，没有改写原始图像；颜色阈值抽取存在边缘抗锯齿误差，不将所有字形逐像素匹配视为通过。

关键交互没有新增问题：radio仍为原生单选，日期切换保持隐藏/显示；部门未换源；分组排序/合计/导出与计数逻辑没有变。最终真实浏览器交互回归由主代理执行。
