# 2026-09-20 AI 截图几何复核

对照输入：用户第 4 图，原始尺寸 3837×2098；右侧实站应用画布原点 `(1927,105)`，应用画布宽 1910 物理像素。根任务本地截图 `renders/aiAssistStats.png` 尺寸 1910×1985，DPR=1.25，应用画布原点 `(0,0)`，可以先去除右侧浏览器边框平移后比较。

## 第一轮真实渲染发现

| 锚点（应用内物理像素） | 实站截图 | 本地第一轮 | 差异 |
| --- | ---: | ---: | ---: |
| 筛选控件顶部 | 169 | 180 | 本地低 11 |
| 表头顶边 | 241 | 244 | 本地低 3 |
| 姓名列首行文本左边 | 437 | 438 | 基本相同 |
| 部门首行文本左边 | 694 | 698 | 本地右 4 |
| AI 直发首行字形左边 | 933 | 950 | 本地右 17 |
| AI 参考首行字形左边 | 1078 | 1098 | 本地右 20 |
| 手动文本首行字形左边 | 1220 | 1242 | 本地右 22 |
| 进度条首行绿色起点 | 1490 | 1499 | 本地右 9 |

实站顶部横滚条位于约 y=213–224。本地第一轮有真实横向溢出但 headless 默认滚动条未绘制；不是截图不存在溢出。

## 据此修改

- 筛选行 58→48 CSS px，padding 从 14/16/10 改为 8/16/12；为真实滚动容器指定 8 CSS px 的 WebKit 横向滚动条。计算后的控件顶边预计 y≈169、表头顶边 y≈241；须等新截图复核。
- 列宽改为 `60/200/150/120/120/120/130/170/140`。之前部门列太宽、辅助合计与辅助率列太窄，导致中部指标右偏。
- 字体改为来源 `--live-font`（Inter → 系统 Segoe UI 等），不再优先 Helvetica/Arial。普通行 41.8 CSS px，直接复用已有 live-ui profile 实测 41.799999 值。
- 进度条在单元格内左对齐；槽宽 86 CSS px、百分比 gap=10 CSS px。
- 从来源原图实测的主色：表头 `#fafbfc`、部门标签底 `#f7f8fa`、正文 `#4a5564`、AI 参考 `#576b95`；绿色 `#07c160` 保持不变。对进度槽做局部精确采色后确认其为 `#edf1f4`，此前全区频率里的 `#ebeef2` 并不是进度槽。

## 第二、三轮复核

- 第二轮直发首行字形 x=933 与来源 x=933 一致，参考 x=1079 对来源 x=1078，手动文本 x=1222 对来源 x=1220，进度条起点 x=1490 与来源一致。
- 第二轮仍少 8 CSS px 的原生横滚条高度。根任务查明原因是 Playwright Chromium 默认 `--hide-scrollbars` 参数；已用 `ignoreDefaultArgs` 排除，未做自绘假滚动条。
- 第三轮表头顶边 y=241 与来源一致；AI 根内容默认纵向滚动条额外占用 15 CSS px，收窄了表格。源 AI 截图没有该根竖条，所以 AI 专用规则隐藏根竖条并保留 wheel / keyboard 滚动，内部横滚条仍显示且联动。
- 快捷日期每边 padding 10.8px、26px 高，后间距8px，修正日期块较来源右偏约22物理像素。维度按钮22px高。
- 来源正文、dept标签和百分比的字号分别为13/11.5/12.5px，这也与归档 `stats_ai_assist_v1.0.html` / `_admin_list.css` 一致。部门标签改为23px行高、上下1px内边距，复原长部门行换行。

## 仍需区分

- 此文件记录实际截图的一轮复核及修正，不声明最终像素验收通过。
- 源图属于 Windows Chrome 实站，可能存在字体栅格和浏览器缩放差别；第一轮已经确认画布宽和 DPR 可对应。
- 侧栏展开/滚动位置及打开的页签是用户会话状态，不把新增销售变声入口删除来伪造同一截图。
- 22 行仅为这张截图完整可见部分；没有采集全量和部门拆分，不把它们改称完整统计。

## 最终本地渲染复核

最终截图：`renders/aiAssistStats.png`，1910×1993 物理像素；已排除 headless 隐藏滚动条参数。AI 根宽恢复到1248 CSS px。

| 首行可读字形/控件锚点 | 来源 (x,y) | 最终本地 (x,y) |
| --- | --- | --- |
| 姓名左上 | (437,310) | (438,311) |
| 部门左上 | (694,311) | (695,314) |
| AI参考左上 | (1078,313) | (1078,314) |
| 手动文本左上 | (1220,313) | (1221,314) |
| AI辅助合计左上 | (1374,310) | (1375,311) |
| AI辅助率左上 | (1610,312) | (1611,314) |
| 成功消息数左上 | (1762,313) | (1759,314) |
| 进度条绿色起点 | (1490,315) | (1490,316) |

这些锚点已收敛到0–3个物理像素的差距。最终首屏布局、颜色、控件类型、列与进度条位置已可对应；仍有字体栅格、表格底部累计约数像素、横滚条滑块长度及滚动会话状态差别，不使用“全页逐像素完全相同”的结论。未再按这些小差异修改业务行为。
